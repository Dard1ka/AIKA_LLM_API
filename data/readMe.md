# data/

Folder ini untuk penyimpanan lokal (local-first):

- `chat.db` : SQLite untuk menyimpan semua chat mentah (user & assistant)
- `chroma/` : Vector database (Chroma) untuk semantic search chat lama

Catatan:
- Folder `data/` sebaiknya tidak di-push ke Git (privacy + size).
